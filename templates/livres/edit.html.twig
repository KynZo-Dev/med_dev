{% extends 'base.html.twig' %}

{% block title %}Edit Livres{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
            <h1>Edit Livres</h1>

            {{ include('livres/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('livres_index') }}">back to list</a>

            {{ include('livres/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
